import { Button } from "@/components/ui/button";

const Page = () => {
  return (
    <div className="font-bold">
      <Button variant="danger">Click Me</Button>
    </div>
  );
};

export default Page;
